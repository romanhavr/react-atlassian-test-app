// import test from 'tape';

// import { asyncData } from './sagas'

// test('asyncData Saga test', (assert) => {
//   const gen = asyncData('some data')

//   assert.deepEqual(
//     gen.next(),
//     call(delay, 2000),
//     // { done: false, value: ??? },
//     'asyncData() should return a Promise that will resolve after 2 seconds'
//   );

//   assert.end()
// });